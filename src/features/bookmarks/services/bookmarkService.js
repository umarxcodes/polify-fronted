import {
  getBookmarks,
  addBookmark,
  removeBookmark,
  checkBookmarkStatus,
  getBookmarkStats,
} from "../api/bookmarksApi";

export const bookmarkService = {
  getBookmarks,
  addBookmark,
  removeBookmark,
  checkBookmarkStatus,
  getBookmarkStats,
};
